const numerador = 1;
let divisor = 0;
let x = 0;

function dividir() {
    if (divisor === 0 && numerador > 1) {
        return "Não é divisivel por 0";
    } else if (dividir === 0 && numerador === 1) {
        return x = 1;
    } else {
        return numerador / divisor;
    }
}

const resultado = dividir()
console.log(resultado);
